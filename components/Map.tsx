import { Text } from 'react-native';
import MapView from 'react-native-maps';

const Map = () => {
  return (
    <MapView className="w-full h-full rounded-2xl">
      <Text>Map</Text>
    </MapView>
  );
};

export default Map;
